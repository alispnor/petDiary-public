[pytest]
DJANGO_SETTINGS_MODULE = petdiary.settings
python_files = tests.py test_*.py *_tests.py
markers =
    smoke: smoke tests E2E do core (login → PIN → claim → revoke → record)
addopts = -ra --tb=short
